{
  "name": "fullstackopen-part3",
  "version": "1.0.0",
  "description": "exercises for part 3 of full stack open",
  "main": "index.js",
  "scripts": {
    "build:ui": "rm -rf static && cd frontend && rm -rf dist && npm run build && mv dist ../static",
    "deploy": "fly deploy",
    "deploy:full": "npm run build:ui && npm run deploy",
    "start": "node src/index.js",
    "test": "echo \"Error: no test specified\" && exit 1",
    "dev": "nodemon src/index.js",
    "lint": "eslint ."
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^16.3.1",
    "express": "^4.18.2",
    "mongoose": "^8.0.1",
    "morgan": "^1.10.0"
  },
  "devDependencies": {
    "@flydotio/dockerfile": "^0.4.11",
    "eslint": "^8.54.0",
    "nodemon": "^3.0.1"
  }
}
