{
  "name": "fullstackopen-part3",
  "version": "1.0.0",
  "description": "exercises for part 3 of full stack open",
  "main": "index.js",
  "scripts": {
    "build:ui": "rm -rf static && cd frontend && npm run build && cp -r dist ../static",
    "deploy": "fly deploy",
    "deploy:full": "npm run build:ui && npm run deploy",
    "start": "node src/index.js",
    "test": "echo \"Error: no test specified\" && exit 1",
    "dev": "nodemon src/index.js"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^4.18.2",
    "morgan": "^1.10.0"
  },
  "devDependencies": {
    "@flydotio/dockerfile": "^0.4.11",
    "nodemon": "^3.0.1"
  }
}
